import { productSchema } from './product';
import { campaignSchema } from './campaign';
import { settingsSchema } from './settings';

export const schemaTypes = [productSchema, campaignSchema, settingsSchema];
